<%= form_for(@v_threshold_type) do |f| %>
  <% if @v_threshold_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_threshold_type.errors.count, "error") %> prohibited this v_threshold_type from being saved:</h2>

      <ul>
      <% @v_threshold_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
