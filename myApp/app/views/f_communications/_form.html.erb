<%= form_for(@f_communication) do |f| %>
  <% if @f_communication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@f_communication.errors.count, "error") %> prohibited this f_communication from being saved:</h2>

      <ul>
      <% @f_communication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ed_logicaldev_id %><br>
    <%= f.text_field :ed_logicaldev_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
