<%= form_for(@v_purpose) do |f| %>
  <% if @v_purpose.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_purpose.errors.count, "error") %> prohibited this v_purpose from being saved:</h2>

      <ul>
      <% @v_purpose.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
