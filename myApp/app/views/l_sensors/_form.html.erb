<%= form_for(@l_sensor) do |f| %>
  <% if @l_sensor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@l_sensor.errors.count, "error") %> prohibited this l_sensor from being saved:</h2>

      <ul>
      <% @l_sensor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sensor_address %><br>
    <%= f.text_area :sensor_address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
