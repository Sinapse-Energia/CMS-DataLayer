<%= form_for(@f_basic) do |f| %>
  <% if @f_basic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@f_basic.errors.count, "error") %> prohibited this f_basic from being saved:</h2>

      <ul>
      <% @f_basic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ed_logicaldev_id %><br>
    <%= f.text_field :ed_logicaldev_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
