<p id="notice"><%= notice %></p>

<h1>Listing F Basics</h1>

<table>
  <thead>
    <tr>
      <th>Ed logicaldev</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @f_basics.each do |f_basic| %>
      <tr>
        <td><%= f_basic.ed_logicaldev %></td>
        <td><%= link_to 'Show', f_basic %></td>
        <td><%= link_to 'Edit', edit_f_basic_path(f_basic) %></td>
        <td><%= link_to 'Destroy', f_basic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New F basic', new_f_basic_path %>
