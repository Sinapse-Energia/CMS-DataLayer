<%= form_for(@v_occurrence) do |f| %>
  <% if @v_occurrence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_occurrence.errors.count, "error") %> prohibited this v_occurrence from being saved:</h2>

      <ul>
      <% @v_occurrence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
