<%= form_for(@v_dynamicoperation) do |f| %>
  <% if @v_dynamicoperation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_dynamicoperation.errors.count, "error") %> prohibited this v_dynamicoperation from being saved:</h2>

      <ul>
      <% @v_dynamicoperation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
