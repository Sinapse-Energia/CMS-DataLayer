<%= form_for(@f_lampactuator) do |f| %>
  <% if @f_lampactuator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@f_lampactuator.errors.count, "error") %> prohibited this f_lampactuator from being saved:</h2>

      <ul>
      <% @f_lampactuator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ed_logicaldev_id %><br>
    <%= f.text_field :ed_logicaldev_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
