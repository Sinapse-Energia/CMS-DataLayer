<%= form_for(@l_lightstate) do |f| %>
  <% if @l_lightstate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@l_lightstate.errors.count, "error") %> prohibited this l_lightstate from being saved:</h2>

      <ul>
      <% @l_lightstate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %><br>
    <%= f.number_field :level %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
