<%= form_for(@v_reason) do |f| %>
  <% if @v_reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_reason.errors.count, "error") %> prohibited this v_reason from being saved:</h2>

      <ul>
      <% @v_reason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
