<p id="notice"><%= notice %></p>

<h1>Listing Vf Lampactuator Events</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vf_lampactuator_events.each do |vf_lampactuator_event| %>
      <tr>
        <td><%= vf_lampactuator_event.name %></td>
        <td><%= link_to 'Show', vf_lampactuator_event %></td>
        <td><%= link_to 'Edit', edit_vf_lampactuator_event_path(vf_lampactuator_event) %></td>
        <td><%= link_to 'Destroy', vf_lampactuator_event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Vf lampactuator event', new_vf_lampactuator_event_path %>
