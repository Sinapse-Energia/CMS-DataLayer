<%= form_for(@vf_lampactuator_event) do |f| %>
  <% if @vf_lampactuator_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vf_lampactuator_event.errors.count, "error") %> prohibited this vf_lampactuator_event from being saved:</h2>

      <ul>
      <% @vf_lampactuator_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
