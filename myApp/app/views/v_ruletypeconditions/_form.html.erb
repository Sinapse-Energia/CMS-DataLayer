<%= form_for(@v_ruletypecondition) do |f| %>
  <% if @v_ruletypecondition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_ruletypecondition.errors.count, "error") %> prohibited this v_ruletypecondition from being saved:</h2>

      <ul>
      <% @v_ruletypecondition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
