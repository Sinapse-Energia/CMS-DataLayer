<%= form_for(@v_typeactiveperiod) do |f| %>
  <% if @v_typeactiveperiod.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_typeactiveperiod.errors.count, "error") %> prohibited this v_typeactiveperiod from being saved:</h2>

      <ul>
      <% @v_typeactiveperiod.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
