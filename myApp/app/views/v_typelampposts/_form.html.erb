<%= form_for(@v_typelamppost) do |f| %>
  <% if @v_typelamppost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_typelamppost.errors.count, "error") %> prohibited this v_typelamppost from being saved:</h2>

      <ul>
      <% @v_typelamppost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :short_name %><br>
    <%= f.text_area :short_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
