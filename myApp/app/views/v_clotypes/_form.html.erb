<%= form_for(@v_clotype) do |f| %>
  <% if @v_clotype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_clotype.errors.count, "error") %> prohibited this v_clotype from being saved:</h2>

      <ul>
      <% @v_clotype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
