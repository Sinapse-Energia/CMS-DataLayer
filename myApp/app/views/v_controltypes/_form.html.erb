<%= form_for(@v_controltype) do |f| %>
  <% if @v_controltype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_controltype.errors.count, "error") %> prohibited this v_controltype from being saved:</h2>

      <ul>
      <% @v_controltype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
