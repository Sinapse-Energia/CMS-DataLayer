<%= form_for(@v_typecommand) do |f| %>
  <% if @v_typecommand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_typecommand.errors.count, "error") %> prohibited this v_typecommand from being saved:</h2>

      <ul>
      <% @v_typecommand.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :short_name %><br>
    <%= f.text_area :short_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
