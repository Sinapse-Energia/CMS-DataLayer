<%= form_for(@v_type_alarm_lamp) do |f| %>
  <% if @v_type_alarm_lamp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_type_alarm_lamp.errors.count, "error") %> prohibited this v_type_alarm_lamp from being saved:</h2>

      <ul>
      <% @v_type_alarm_lamp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
