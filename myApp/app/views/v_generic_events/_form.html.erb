<%= form_for(@v_generic_event) do |f| %>
  <% if @v_generic_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_generic_event.errors.count, "error") %> prohibited this v_generic_event from being saved:</h2>

      <ul>
      <% @v_generic_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
