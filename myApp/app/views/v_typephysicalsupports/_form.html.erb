<%= form_for(@v_typephysicalsupport) do |f| %>
  <% if @v_typephysicalsupport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_typephysicalsupport.errors.count, "error") %> prohibited this v_typephysicalsupport from being saved:</h2>

      <ul>
      <% @v_typephysicalsupport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :short_name %><br>
    <%= f.text_area :short_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
