<%= form_for(@v_typecommunication) do |f| %>
  <% if @v_typecommunication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@v_typecommunication.errors.count, "error") %> prohibited this v_typecommunication from being saved:</h2>

      <ul>
      <% @v_typecommunication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
